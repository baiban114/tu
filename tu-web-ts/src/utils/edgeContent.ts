/** @deprecated Use `@/utils/cellContent` — kept for existing imports. */
export {
  type CellContentBinding as EdgeContentBinding,
  emptyCellContentDocument as emptyEdgeContentDocument,
  readCellContentBinding as readEdgeContentBinding,
  isCellContentBound as isEdgeContentBound,
} from '@/utils/cellContent'
